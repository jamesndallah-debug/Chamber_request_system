# Copy this file to .env and fill in values

# Application
BASE_URL=http://localhost/chamber_request_system

# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=chamber_request_system

# Google OAuth 2.0
# Get these from Google Cloud Console -> APIs & Services -> Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Mail sender (used by registration emails and notifications)
MAIL_FROM_EMAIL=no-reply@yourdomain.tld
MAIL_FROM_NAME=Chamber Request System
