# Copy this file to .env and fill in values

# Application
BASE_URL=http://localhost/chamber_request_system

# Database
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=chamber_request_system

# Google OAuth 2.0
# Get these from Google Cloud Console -> APIs & Services -> Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Mail sender (used by registration emails and notifications)
MAIL_FROM_EMAIL=no-reply@yourdomain.tld
MAIL_FROM_NAME=Chamber Request System

# SMTP (optional but recommended). If provided, emails will be sent via SMTP.
# Common examples:
# - Gmail/Google Workspace: SMTP_HOST=smtp.gmail.com, SMTP_PORT=465, SMTP_SECURE=ssl, SMTP_USER=you@domain.tld, SMTP_PASS=app_password
# - Mail servers (cPanel, etc.): Use the host/port from your provider; prefer port 465 with SSL.
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
# SMTP_SECURE supports: '' (plain) or 'ssl' (implicit TLS on port 465). STARTTLS is not supported by the built-in client.
SMTP_SECURE=
